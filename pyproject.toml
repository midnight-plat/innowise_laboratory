[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

dependencies = [
    "fastapi==0.104.1",
"sqlalchemy>=2.0.0",
"uvicorn~=0.20.0",
"pydantic==2.5.3"  # установка версии бибилиотеки

]

[project]
authors = [
  {name = "Viktor", email = "viktor@example.com"}
]